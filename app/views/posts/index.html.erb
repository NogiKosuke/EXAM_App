<h2>出来事<h2>

<table>
  <tr>
    <th>内容</th>
  </tr>

  <% @posts.each do |post| %>
    <tr>
      <td><%= post.content %></td>
      <td><%= link_to "詳細を確認する", post_path(post.id) %>
    </tr>
  <% end %>
</table>
<%= link_to "出来事を追加する", new_post_path %>