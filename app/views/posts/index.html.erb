<h2>日頃の出来事を登録しよう！<h2>

<table>
  <tr>
    <th>内容</th>
  </tr>

  <% @posts.each do |post| %>
    <tr>
      <td><%= post.content %></td>
      <td><%= link_to "詳細", post_path(post.id) %></td>
      <td><%= link_to "編集", edit_post_path(post.id) %></td>
      <td><%= link_to "削除", post_path(post.id), method: :delete %>
    </tr>
  <% end %>
</table>
<%= link_to "出来事を追加する", new_post_path %>